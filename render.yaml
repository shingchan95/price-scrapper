services:
  - type: web
    name: price-scrapper
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/app.py
    build:
      # Tell Render to use `backend` as the working directory
      cwd: backend
    autoDeploy: true
